[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "chromium"]

[phases.install]
cmds = ["npm install"]

[variables]
# Point puppeteer-core to system Chromium
PUPPETEER_EXECUTABLE_PATH = "/nix/var/nix/profiles/default/bin/chromium"

[start]
cmd = "node server.js"
